# DENTYAR V31 — گزارش اصلاحات

## 1) خروجی Excel/CSV در Android
مشکل اصلی نسخه قبلی این بود که در Android، حالت `share:false` فایل را فقط در CACHE خصوصی برنامه می‌نوشت و پیام «در Documents/DENTYAR ذخیره شد» نمایش می‌داد؛ در نتیجه کاربر عملاً فایل را در پوشه قابل مشاهده دریافت نمی‌کرد.

در V31:
- خروجی‌های CSV و Excel ابتدا در فضای امن برنامه ساخته می‌شوند.
- URI واقعی فایل از Capacitor Filesystem گرفته می‌شود.
- فایل از طریق Android Sharesheet به کاربر تحویل می‌شود تا در Downloads/Files/Drive یا برنامه مقصد ذخیره شود.
- لغو Share به‌درستی تشخیص داده می‌شود.
- متن پیام‌های برنامه با رفتار واقعی هماهنگ شده است.
- طراحی و پنل‌های برنامه تغییر نکرده‌اند.

همین مسیر برای فایل پشتیبان و فایل انتقالی نیز اعمال شده است.

## 2) رفع گیر پرونده بیمار
متغیر `openPatientId` در نسخه قبلی هنگام خروج از بخش «بیماران» پاک نمی‌شد و ممکن بود با ورود دوباره به بخش بیماران، پرونده قبلی باز بماند.

در V31 هنگام رفتن به هر بخش غیر از «بیماران»، شناسه پرونده بازشده پاک می‌شود.

## 3) کنترل فنی
- تمام اسکریپت‌های inline با `node --check` بررسی شدند.
- خطای syntax مشاهده نشد.
- نسخه پروژه به V31 ارتقا داده شد.

## 4) وضعیت آنلاین چندکاربره
این فایل هنوز یک برنامه محلی است و در ZIP هیچ پروژه Firebase/Firestore، Supabase یا Backend متصل وجود ندارد. بنابراین «آنلاین چندکاربره واقعی» را نمی‌توان بدون ایجاد/اتصال یک پروژه ابری فعال کرد.

این محدودیت را عمداً جعل یا دور نزدم؛ فعال‌سازی واقعی نیازمند شناسه پروژه و تنظیمات سرویس ابری متعلق به صاحب سامانه است. پس از فراهم شدن تنظیمات پروژه، لایه همگام‌سازی می‌تواند بدون تغییر ظاهر و پنل‌های فعلی روی همین هسته اضافه شود.
